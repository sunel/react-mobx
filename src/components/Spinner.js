import React, { PureComponent } from 'react';

class Spinner extends PureComponent {
    render() {
        return (
            <div>
                Cool
            </div>
        )
    }
}

export default Spinner;